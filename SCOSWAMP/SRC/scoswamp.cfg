# Configuration de lien de SCOSWAMP : apple2enh.cfg de cc65 2.19, plus une
# zone de RAM basse.
#
# LOWRAM : $1000-$1FFF, entre le tampon d'E/S ProDOS ($0800-$0BFF) et HGR
# page 1 ($2000). Le programme
# charge en $4000, cette zone etait libre et le lieur l'ignorait. Le segment
# LOWBSS y recoit les gros tampons (catalogue des messages, tampon de page,
# tampon du decodeur HGR, barre de titre) : ~3,8 Ko rendus a la fenetre
# principale.
#
# Deux regles a ne pas oublier :
#  - crt0 ne met a zero que le segment BSS ; main() efface LOWBSS lui-meme
#    avec __LOWBSS_RUN__ / __LOWBSS_SIZE__ (define = yes) ;
#  - le tas suit la BSS *principale* (__BSS_RUN__ + __BSS_SIZE__), pas
#    LOWBSS -- c'est ce qui rend ce segment sur, la ou reloger toute la BSS
#    en RAM basse enverrait malloc() dans HGR page 1 (voir DOCS/MEMOIRE.md).
#  - JAMAIS PLUS D'UN FICHIER OUVERT A LA FOIS. iobuf-0800 distribue ses
#    tampons de 1 Ko a partir de $0800 vers le haut : le premier prend
#    $0800-$0BFF, un second prendrait $0C00-$0FFF -- que la zone MAPRAM
#    occupe desormais. Le jeu n'en ouvre qu'un (chaque fopen est suivi de son
#    fclose avant le suivant : texte, image, musique, aide, sauvegarde,
#    catalogue, carte). Ouvrir deux fichiers ecraserait la carte.
#
# MAPRAM : $0C00-$0FFF, le kilo-octet que l'ancienne note reservait a ce
#    second tampon. Le segment MAPBSS y loge les donnees du menu MAP -- 125
#    octets d'en-tete et de clairieres, puis le bloc de langue -- lues du
#    fichier MAP par map_load(). Ce sont 1 024 octets qui ne coutent RIEN a la
#    fenetre principale, ou il ne restait que 510 octets de tas.
FEATURES {
    STARTADDRESS: default = $4000;
}
SYMBOLS {
    __EXEHDR__:    type = import;
    __FILETYPE__:  type = weak, value = $0006; # ProDOS file type
    __STACKSIZE__: type = weak, value = $0800; # 2k stack
    __HIMEM__:     type = weak, value = $9600; # Presumed RAM end
    __LCADDR__:    type = weak, value = $D400; # Behind quit code
    __LCSIZE__:    type = weak, value = $0C00; # Rest of bank two
}
MEMORY {
    ZP:     file = "", define = yes, start = $0080,        size = $001A;
    HEADER: file = %O,               start = %S - $003A,   size = $003A;
    MAPRAM: file = "", define = yes, start = $0C00,        size = $0400;
    LOWRAM: file = "", define = yes, start = $1000,        size = $1000;
    LCIMAGE: file = %O, define = yes, start = $1000, size = $0C00, fill = yes;
    MAIN:   file = %O, define = yes, start = %S,           size = __HIMEM__ - %S;
    BSS:    file = "",               start = __ONCE_RUN__, size = __HIMEM__ - __STACKSIZE__ - __ONCE_RUN__;
    LC:     file = "", define = yes, start = __LCADDR__,   size = __LCSIZE__;
}
SEGMENTS {
    ZEROPAGE: load = ZP,             type = zp;
    EXEHDR:   load = HEADER,         type = ro,  optional = yes;
    STARTUP:  load = MAIN,           type = ro;
    LOWCODE:  load = MAIN,           type = ro,  optional = yes;
    CODE:     load = MAIN,           type = ro;
    RODATA:   load = MAIN,           type = ro;
    DATA:     load = MAIN,           type = rw;
    INIT:     load = MAIN,           type = rw;
    ONCE:     load = MAIN,           type = ro,  define   = yes;
    LC:       load = LCIMAGE, run = LC, type = ro, optional = yes;
    BSS:      load = BSS,            type = bss, define   = yes;
    LOWBSS:   load = LOWRAM,         type = bss, define   = yes, optional = yes;
    MAPBSS:   load = MAPRAM,         type = bss, define   = yes, optional = yes;
}
FEATURES {
    CONDES: type    = constructor,
            label   = __CONSTRUCTOR_TABLE__,
            count   = __CONSTRUCTOR_COUNT__,
            segment = ONCE;
    CONDES: type    = destructor,
            label   = __DESTRUCTOR_TABLE__,
            count   = __DESTRUCTOR_COUNT__,
            segment = RODATA;
    CONDES: type    = interruptor,
            label   = __INTERRUPTOR_TABLE__,
            count   = __INTERRUPTOR_COUNT__,
            segment = RODATA,
            import  = __CALLIRQ__;
}
